Quick Look Demo
===============

This reStructuredText sample checks headings, paragraphs, literal blocks, and lists.

Features
--------

* YAML and Terraform preview through a Quick Look extension.
* Native text selection and copy behavior.
* Lightweight syntax highlighting returned as RTF.

Example::

    let preview = "native Quick Look renderer"
    print(preview)

The goal is boring, stable preview behavior.
